import { CategorizarPipe } from './categorizar.pipe';

describe('CategorizarPipe', () => {
  it('create an instance', () => {
    const pipe = new CategorizarPipe();
    expect(pipe).toBeTruthy();
  });
});
